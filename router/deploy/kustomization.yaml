apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - role.yaml
  - rolebinding.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - service-pods.yaml

# Uncomment to set a specific namespace
# namespace: rkllama

# Uncomment to set a specific image tag
# images:
#   - name: ghcr.io/rsjames-ttrpg/rkllama-router
#     newTag: v1.0.0
